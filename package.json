{
  "name": "occlude-workspace",
  "private": true,
  "scripts": {
    "build:wasm": "cd crates/occlude-core && wasm-pack build --target web --out-dir pkg --features wasm --no-default-features",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  }
}

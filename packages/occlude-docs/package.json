{
  "name": "occlude-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter occlude docs:signatures && blume dev",
    "build": "pnpm --filter occlude docs:signatures && blume build",
    "preview": "blume preview"
  },
  "devDependencies": {
    "blume": "^1.7.0"
  }
}

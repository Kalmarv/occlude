# Implementation commits

Branch `feat/3d-webgpu`; each progress commit pushed to `origin/dev`. Starting commit `c705cdeb6a575eecc2759572e8d43dbb8e4316d2`. The handoff documentation commit follows this list.

- `83f99f5` Establish isolated 3D development environment and implementation contracts
- `d4d2ed5` Add hardware-verified WebGPU visibility and camera walking skeleton
- `ca8f242` Move 3D viewport and visibility into a revision-aware GPU worker
- `4e8c1fa` Add topology-derived mesh visibility and hardware browser fixtures
- `ab40e6c` Construct classified strokes and preserve authored runs through planning
- `746a41a` Add editable extrusion passes and batched GPU deformation
- `b4239be` Add prepared batched surface queries and query-driven modeling
- `d4a05f5` Add explicit async sketches and serialize Studio worker execution
- `0df7042` Resolve deferred 3D scenes through the Studio GPU worker
- `b6d5c1c` Expose GPU deformation and surface queries on the async toolkit
- `df38852` Add captured point and edge selections and transformed instances
- `6053a2f` Expose reusable 3D classification and independent stroke styling
- `ced755e` Preserve source-chain modifier phase through visibility cuts
- `40d83c0` Generate source-supported mesh plane sections
- `46c4702` Generate physical paper hatch and crosshatch on captured surfaces
- `b7d1756` Serve isolated dev bundle with Studio storage APIs
- `27f1dc4` Add retained GPU construction view and face picking in Studio
- `8b42acb` Preserve committed 3D inputs in frozen saved results
- `70aa130` Measure large-scene CPU GPU visibility and orbit baseline
- `87b1208` Retain world geometry on GPU for interactive Studio orbit
- `e0b0e7f` Retain paper composition for camera commits without remodeling
- `ec95b08` Commit explored Studio cameras against retained drawing composition
- `82a5078` Persist committed 3D cameras in portable sketch configuration
- `0c6b91c` Remove numerical false creases from flat 3D surfaces
- `faca614` Verify CPU and GPU line coverage against pinned Blender 5.2.1
- `9cbf24b` Stage Studio renders until the current client accepts publication
- `a53da68` Verify imperial 3D composition with portable pen and paper imports
- `4a17683` Package verified visibility and seeded relief demonstration sketches
- `5a1d82d` Capture pinned Freestyle vectors and document reference differences
- `b4a0cba` Measure visibility compute passes with optional GPU timestamps
- `c91c1ca` Keep 3D stroke randomness stable across source selection order
- `e005792` test(3d): verify analytical precision cases on CPU and hardware GPU
- `566070e` fix(3d): derive GPU refinement tolerance from paper and nib scale
- `20347d9` test(3d): verify GPU cancellation and interleaved execution isolation

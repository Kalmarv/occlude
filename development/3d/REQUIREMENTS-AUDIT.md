# M0–M5 working acceptance audit

Scope: every requirement in [spec.md](spec.md), with subsequent user-approved deferrals recorded in [USER-FOLLOWUPS.md](USER-FOLLOWUPS.md). The original specification file remains unchanged. This is a working audit, not a completion certificate. Existing implementation/evidence is listed to guide targeted review; a referenced test name alone does not prove every clause.

## Remaining acceptance priorities

| Requirement | Current evidence and remaining work |
| --- | --- |
| §4, §16 pinned Blender comparisons | Eight CPU and hardware-GPU/Line Art vector comparisons now pass against pinned Blender 5.2.1; see reference/README.md. Freestyle selection/constant-style references now cover the same fixtures; six match and two intersecting-box discrepancies are explicitly retained, with Line Art and independent rays supporting Occlude (reference/FREESTYLE.md). |
| §6, §9, §16 abandoned work never adopts | Laboratory worker has lease/revision tests and hardware cancellation/restart evidence. Main Studio now stages publication and passes real-GPU late-completion/newer-failed-render checks for ordinary renders and camera commits (STUDIO-ADOPTION.md). Audit remaining compute cancellation boundaries. Hard worker restart/export recovery and actual GPU device-loss recovery are explicitly deferred by the user; page reload is the accepted workaround. |
| §12 GPU timing when supported | Optional visibility compute-pass timestamps now propagate through scene stats and the benchmark. Served hardware checks cover timed/untimed and capability/budget fallback, repeated readback and memory/transfer accounting. TIMESTAMPS.md distinguishes kernel and full-pipeline time. |
| §15 visibility laboratory demo | Complete named demo in demos/visibility-laboratory.ts; main Studio hardware evidence covers all required objects, cached clean/dashed/wobbled/filtered interpretations, console stroke inspection/counters and camera commit. See demos/README.md. |
| §15 procedural city/relief demo | Complete named demo in demos/procedural-relief.ts; main Studio hardware evidence verifies seeded site selection, varying extrusion, attribute deformation, effective query edits, attribute hatch spacing, sections and repeatability. See demos/README.md. |
| §15 paper composition demo; §16 downloaded imports | Complete named imperial-sheet demo and main Studio hardware download/reopen pass: PAPER-COMPOSITION.md. Two instances of an imported pen, a one-off pen, labels/mask, committed camera and bundled pen/paper definitions retain exact plan/settings/SVG under changed local libraries. |
| §16 precision and adversarial completeness | Analytical intervals, independent rays, all-pairs index checks, strong foreshortening, thin occluders, near clipping, mirroring and many degeneracies exist. Audit each listed adversarial case against exact test assertions and hardware coverage, including world-scale extremes, coincident topology and triangle-boundary contacts. |
| §10 source/style/pass identity | Found and fixed row-dependent decimation. Stable source/line-set/pen/pass keys now cross the TS/WASM boundary; planned-output regressions cover reordering, unrelated-source removal, ordered wobble/decimation, pass and sketch seed changes. See STYLE-IDENTITY.md. Repetition generators remain deferred; explicit pass IDs distinguish existing interpretations. |
| §16 performance | CPU/GPU complete pipeline and large-scene overlap/overflow measurements exist; retained main Studio orbit meets roughly 30 FPS on RTX 2060. Current served visibility/retained-viewport benchmark, timing/memory coverage and limitations are recorded in TIMESTAMPS.md; consolidate with the final requirement audit and handoff. |
| §18 final handoff | Three final demos and representative SVG exports now packaged in demos/README.md. Still needs complete requirement mapping, consolidated architecture/ownership/cancellation/precision note, branch/commit list and final production-isolation checks. |

## Implemented areas requiring final scope-matched signoff

- §3/§7/§8 geometry: polygon/box/grid/point-cloud/open-wire data; stable IDs/attributes/adjacency; owned snapshots; independent nonadjacent extrusion with cap/side parentage; signed/zero distances; intensive/extensive transfer; frozen passes/history; point/edge/face selections; transforms and mirrored winding. Tests: three-model, three-selection, three-mesh. Pending signoff includes every unsupported/ambiguous geometry diagnostic and extreme-scale clause.
- §8/§9 modeling execution: additive async compiler/toolkit, batched GPU displacement and gather relaxation, ray/segment/nearest queries with owned readback, closed-execution guards. Tests: three-deform, three-queries, three-toolkit; hardware docs reports. Verify interleaved runs with all required differing inputs, not only isolated successful batches.
- §7/§11 camera/visibility: RH Z-up, explicit camera basis, WebGPU depth, near/far clipping, physical paper projection, conservative projected index, bounded streaming, f32 uncertainty/f64 refinement, support provenance, hidden/visible unions. Tests: three-visibility, three-mesh; hardware laboratory/benchmark reports. Exact-case audit remains open as listed above.
- §10 feature/stroke pipeline: boundary/silhouette/crease/marked/wire/section/hatch flags, overlapping line-set interval priority, immutable classified selections, source continuity, ambiguous junctions, closed-loop anchors, chain-length filtering and data inspection. Crease numerical correctness now has camera-invariant regressions. Artistic crease-tolerance control is explicitly deferred by the user.
- §10/§14 physical styling: named pens, applicable ordinary modifiers, source-phase continuity, overscan without page-side culling, 2D composition order, protected gaps across recording/WASM/planning/export. Tests: three-strokes, three-style, three-phase, three-scene, three-camera-commit. Audit unsupported/unbounded modifiers and exact phase/identity clauses before final signoff.
- §12 viewport: retained GPU world buffers, uniform-only orbit, source-face picking, no model rerun during exploration; final paper/export stays committed. Main Studio Playwright proves normal flow. Camera-control redesign is deferred; the existing commit path and all invariants still need lifecycle signoff.
- §13 hatch/sections: physical paper lattice lifted onto source support, shared face phase, crosshatch, per-face attributes, folded-surface piecewise interpolation, near clipping, mesh-plane sections. Tests: three-hatch/three-section; live docs and hardware reports. Curvature-following hatch and intersection-curve drawing are post-M5 per the original spec and user discussion.
- §9/§16 persistence: committed camera configuration, realized geometry/backend/seed/precision provenance, exact plan/physical paper/color/pens/timing after changed-library reopen. Source and served NVIDIA Playwright evidence exists. Imported-definition download case now passes in PAPER-COMPOSITION.md.
- §5/§16/§17 isolation/build: worktree `/home/kalmarv/containers/occlude-3d`, isolated compose project/store/port, ordinary CPU import has no GPU initialization, full container `pnpm check`, unchanged original 2D docs ink, church oracle. Latest verified slice is `0c6b91c`; later slices must run their required checks and final isolation inspection.

A row moves to complete only after its full original clauses and concrete acceptance artifacts are inspected. Missing evidence remains pending; deferred post-MVP features must not be silently promoted into MVP work or used to replace required deliverables.
